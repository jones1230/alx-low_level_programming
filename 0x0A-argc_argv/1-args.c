#include <stdio.h>
#include <stdlib.h>

/**
 * main - Takes two arguments
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	printf("%d", argc);
	return (0);
}

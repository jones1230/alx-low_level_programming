#include <stdio.h>

/**
 *main - Prints all alphabets in lower case
 *
 *Return: Always 0(Sucess)
 */
int main(void)
{
	char l;
	char u;

	for (l = 'a'; a <= 'z'; a++)
	{
		putchar(l);
	}
	for (u = 'A'; a <= 'Z'; A++)
	{
		putchar(u);
	}
	putchar('\n');
	return (0);
}

